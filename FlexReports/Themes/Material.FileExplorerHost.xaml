<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FlexReports.Themes"
                    xmlns:fileExplorer="clr-namespace:FlexReports.MaterialControls.FileExplorer">
	
  <Style TargetType="fileExplorer:FileExplorerHost">
    <Setter Property="DataContext" Value="{Binding RelativeSource={RelativeSource Self}}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type fileExplorer:FileExplorerHost}">
          <Grid>
            <!--<ContentPresenter x:Name="cp" RenderTransformOrigin=".5,.5"/>-->
            <ScrollViewer Style="{StaticResource FavsScrollViewer}">
              <ItemsControl ItemsSource="{Binding FileList}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
              </ItemsControl>
            </ScrollViewer>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>