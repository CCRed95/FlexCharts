<Window x:Class="FlexReports.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://www.flexcharts.com/schemas/core"
        xmlns:materialControls="clr-namespace:FlexReports.MaterialControls"
        mc:Ignorable="d" 
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
				TextOptions.TextRenderingMode="Aliased"
        Title="TestFlexDocumentViewer" Height="200" Width="1000">
	<Grid>
		
	</Grid>

</Window>
<!--<DockPanel>
            -->
<!-- TODO Title Bar #8 -->
<!--
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Panel.ZIndex="8">
                <Border Height="18" Background="{Binding TitleBarTheme, Converter={StaticResource MBSTBC}, 
				ConverterParameter={x:Static materialDesign:Luminosity.P900}, FallbackValue={StaticResource mtBlue900}}"/>
                <Grid>
                    <Rectangle Effect="{StaticResource shadowDelta3}" Width="{Binding ElementName=DocumentTabMenu, Path=ActualWidth}" 
									HorizontalAlignment="Right" Fill="{Binding TitleBarTheme, Converter={StaticResource MBSTBC}, 
									ConverterParameter={x:Static materialDesign:Luminosity.P800}, FallbackValue={StaticResource mtBlue800}}"/>
                    <Rectangle Fill="{Binding TitleBarTheme, Converter={StaticResource MBSTBC}, 
				ConverterParameter={x:Static materialDesign:Luminosity.P800}, FallbackValue={StaticResource mtBlue800}}" 
									Height="70" Effect="{StaticResource shadowDelta2}"/>
                    <Label Content="Tests Report Document Viewer" Style="{StaticResource Text16p}" Foreground="White" FontWeight="Medium" 
							 Padding="30 0 10 0" HorizontalAlignment="Left"/>
                    <DockPanel Margin="400 0 0 0" >
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">

                        </StackPanel>
                        -->
<!--<Button DockPanel.Dock="Right" Style="{StaticResource MaterialIcon}" FontSize="24pt" HorizontalAlignment="Right" 
										HorizontalContentAlignment="Right" Content="ic_color_lens" Click="ColorPalatteClicked" Margin="0 0 10 0"/>-->
<!--
                        <Path Style="{StaticResource drawable-close}" MouseUp="ColorPalatteClicked" HorizontalAlignment="Right" Margin="0 0 15 0">
                            <Path.LayoutTransform>
                                <ScaleTransform ScaleX="1.7" ScaleY="1.7"/>
                            </Path.LayoutTransform>
                        </Path>
                    </DockPanel>
                </Grid>

            </StackPanel>
            -->
<!-- TODO Main Bar #7 -->
<!--
            <DockPanel DockPanel.Dock="Bottom" Panel.ZIndex="7">
                -->
<!-- TODO Main Bar #7 -->
<!--
                <Grid DockPanel.Dock="Left" Width="300">
                    -->
<!-- TODO Left Pane #6 -->
<!--
                    <StackPanel Orientation="Vertical" Background="#282828">
                        -->
<!-- TODO Recent Documents #5 -->
<!--
                        <Grid Height="100" Panel.ZIndex="5" Effect="{StaticResource shadowDelta2}" ClipToBounds="True">
          
                            <Label Content="Recent Reports" Style="{StaticResource Text16p}" Foreground="White"
										 VerticalContentAlignment="Bottom" HorizontalContentAlignment="Left" Padding="40 0 0 20"/>
                        </Grid>
                        -->
<!-- TODO List of Documents #4-->
<!--
                        <StackPanel Orientation="Vertical" Panel.ZIndex="4" Effect="{StaticResource shadowDelta1}">
                            -->
<!-- TODO Inner Recent Documents Panel -->
<!--
                            <materialControls:MaterialButtonList x:Name="recentReports"  Orientation="Vertical" Margin="0 20"/>
                        </StackPanel>
                    </StackPanel>
                    -->
<!-- TODO + Button #8 -->
<!--
                    <Grid VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,80,20,0" Panel.ZIndex="10">
                        <Ellipse Width="40" Height="40" Fill="{Binding TitleBarTheme, Converter={StaticResource MBSTBC}, 
						ConverterParameter={x:Static materialDesign:Luminosity.P500}, FallbackValue={StaticResource mtBlue500}}" 
									 Effect="{StaticResource shadowDelta4}"/>
                        <Rectangle Width="2" Height="20" Fill="White"/>
                        <Rectangle Width="20" Height="2" Fill="White"/>
                    </Grid>
                </Grid>
                -->
<!-- TODO Main Bar #7 -->
<!--
                <DockPanel  DockPanel.Dock="Right" Background="{StaticResource mtGrey400}">

                    <Grid Height="30" DockPanel.Dock="Top" Background="{Binding TitleBarTheme, Converter={StaticResource MBSTBC}, 
				ConverterParameter={x:Static materialDesign:Luminosity.P900}, FallbackValue={StaticResource mtBlue900}}"
								 Effect="{StaticResource shadowDelta2}">
                        <materialControls:MaterialTabControl x:Name="DocumentTabMenu" TabChangeRequestedEvent="TabChangeRequested"/>
                    </Grid>
                    <materialControls:DocumentViewport DockPanel.Dock="Bottom" x:Name="documentPanel">


                    </materialControls:DocumentViewport>

                </DockPanel>
            </DockPanel>
        </DockPanel>
        <materialControls:MaterialDimmer x:Name="dimmer" IsDimmed="False"/>
        <Grid x:Name="PopupSpace">

        </Grid>-->